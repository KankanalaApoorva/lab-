#include<stdio.h>
#include<conio.h>
void main()
{
int i,num,sum=0;
clrscr();
printf("Enter the number of odd numbers whose sum of squares has to be calculated\n");
scanf("%d",&num);
for(i=1;i<=num;i++)
sum=sum+(2*i-1)(2*i-1);
printf("The sum of first %d odd numbers is %d",num,sum);
getch();
}
